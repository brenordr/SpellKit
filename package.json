{
  "name": "spellkit",
  "version": "0.0.0",
  "module": "dist/src/index.js",
  "types": "dist/types/core/index.d.ts",
  "type": "module",
  "scripts": {
    "bundle": "bun build ./lib/core/index.ts --outdir ./dist/src --minify --external react --spliting",
    "types": "tsc  --declaration --emitDeclarationOnly --outDir dist/types",
    "test": "bun test",
    "build": "bun run bundle && bun run types",
    "prepublish": "bun run build"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "@types/react": "^18.2.21",
    "react": "^18.2.0"
  }
}
